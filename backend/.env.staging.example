# Staging is local Docker for now — see docker-compose.yml and backend/.env.example.

# Optional: duplicate production Heroku app for a paid second environment (~$12/mo extra).



ENVIRONMENT=local

DEBUG=True

EMAIL_BACKEND=local

FRONTEND_URL=http://localhost:3005

